# TradeImpact Rules (`src/rules`)

Pure financial intelligence database + engine utilities.

See `docs/rules/RULE_ENGINE.md` and `docs/rules/RULE_COVERAGE_REPORT.md`.

```ts
import {
  getRuleRegistry,
  resolveRule,
  resolveConflicts,
  generateCoverageReport,
} from "@/rules";
```
