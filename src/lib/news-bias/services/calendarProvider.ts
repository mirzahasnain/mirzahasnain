/**
 * Calendar provider registry — switch implementations via CALENDAR_CONFIG.
 */
export {
  getCalendarProvider,
  getLiveNewsProvider,
  CALENDAR_PROVIDERS,
  LIVE_PROVIDERS,
} from "../calendar/providers";
