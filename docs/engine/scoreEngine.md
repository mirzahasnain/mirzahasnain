# Score Engine

## Purpose

Compute TradeImpact Score™ + Reliability.

## Inputs

`ScoreInput` — see `src/engine/scoreEngine/types.ts`.

## Outputs

`ScoreResult` — structured object (never a plain string primary return).

## Process API

`process*` in `src/engine/scoreEngine/process.ts`.

## Configuration

JSON under `src/engine/scoreEngine/data/`.

## Tests

`src/engine/scoreEngine/__tests__/`

## Future Improvements

- Pre-release provisional scoring
