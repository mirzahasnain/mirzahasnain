# Scenario Engine

## Purpose

Best / Expected / Worst move scenarios.

## Inputs

`ScenarioInput` — see `src/engine/scenarioEngine/types.ts`.

## Outputs

`ScenarioResult` — structured object (never a plain string primary return).

## Process API

`process*` in `src/engine/scenarioEngine/process.ts`.

## Configuration

JSON under `src/engine/scenarioEngine/data/`.

## Tests

`src/engine/scenarioEngine/__tests__/`

## Future Improvements

- Percentile bands from richer history
